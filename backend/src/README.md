# Backend Source Layout

推荐从这里开始补全实现：

- `app/`：启动入口、配置、日志、插件装配
- `modules/boards/`：板块相关
- `modules/personas/`：角色与身份模板相关
- `modules/threads/`：主题帖与楼层只读接口
- `modules/admin/`：内部管理接口
- `modules/generation/`：内容生成流水线
- `modules/assets/`：头像、表情、徽章资源
- `cli/`：命令行入口
- `lib/`：数据库、解析、校验、通用工具
