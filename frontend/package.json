{
  "name": "llm-bbs-frontend",
  "version": "0.1.0",
  "private": true,
  "description": "Frontend skeleton for a read-only LLM-generated retro BBS project.",
  "scripts": {
    "dev": "echo \"Define frontend dev command after choosing runtime/tooling\"",
    "build": "echo \"Define frontend build command\"",
    "test": "echo \"Add frontend tests\""
  }
}
